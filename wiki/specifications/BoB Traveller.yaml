swagger: '2.0'
info:
  version: 3.0.0
  title: BoB.Traveller.API
  description: |
    The Traveller API provides functionality for other participants to perform
    operations on a traveller's account. The Traveller API is normally provided
    by the sales channel, that has the customer relation and maintains the
    traveller account database. Functionality supported are operations on the
    traveller's account information, operations on a traveller's wallet and
    tokens, to send notifications to a traveller through the sales channel, to
    initiate purchase of a product on the travellers' behalf and to retrieve
    information on products purchased by the traveller.
tags:
  - name: traveller
    description: Functionality relating to traveller information
  - name: wallet
    description: Functionality to retrieve wallet information and to perform wallet transactions
  - name: notification
    description: Functionality relating to traveller notifications
  - name: token
    description: Functionality for ID-based travelling to lookup products tied to a token, to purchase a new product tied to the token and to perform transactions on a wallet tied to a token.
  - name: mtb
    description: Functionality used for looking up product sets within a MTB and to activate all products in a MTB.
  - name: productSet
    description: Functionality mainly used to renew a previously purchased (but now expired) product.
  - name: ticketNotification
    description: Call to indicate that the status of a ticket may have changed.
basePath: /api/v3
schemes:
  - https
paths:
  '/traveller':
    get:
      tags:
        - traveller
      summary: Search for traveller
      description: |
        Get information on all travellers matching the search critera 
        specified by the parameters. 
      operationId: searchTravellerInformation
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: name
          description: Filter on name
          in: query
          required: false
          type: string
        - name: email
          description: Filter on email address
          in: query
          required: false
          type: string
          format: email
        - name: phone
          description: Filter on phone number
          in: query
          required: false
          type: string
        - name: dob
          description: Filter on date of birth
          in: query
          required: false
          type: string
          format: date
        - name: tokenId
          in: query
          description: Filter on token ID
          required: false
          type: string
      responses:
        '200':
          description: Successful operation
          schema:
            type: array
            items:
              $ref: '#/definitions/travellerInformation'
    post:
      tags:
        - traveller
      summary: Create traveller
      description: |
        Create a new traveller with the specified information.
      operationId: createTravellerInformation
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerInformation
          in: body
          description: Traveller information
          required: true
          schema:
            $ref: '#/definitions/travellerInformation'
      responses:
        '201':
          description: Traveller created
          schema:
            $ref: '#/definitions/travellerInformation'
          headers:
            location:
              description: URL of traveller information
              type: string
              format: uri
  '/traveller/{travellerId}':
    get:
      tags:
        - traveller
      summary: Get traveller information
      description: |
        Get information on the traveller with the specified traveller 
        identifier.
      operationId: getTravellerInformation
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerId
          in: path
          description: Traveller's ID
          required: true
          type: string
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/travellerInformation'
    put:
      tags:
        - traveller
      summary: Update traveller information
      description: |
        Update traveller information for traveller with the specified traveller
        identifier.
      operationId: updateTravellerInformation
      consumes:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerId
          in: path
          description: Traveller's ID
          required: true
          type: string
        - name: travellerInformation
          in: body
          description: Traveller information to be updated
          required: true
          schema:
            $ref: '#/definitions/travellerInformation'
      responses:
        '204':
          description: Successful operation
    delete:
      tags:
        - traveller
      summary: Delete traveller information
      description: |
        Delete the traveller with the provided traveller identifier.
      operationId: deleteTravellerInformation
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerId
          in: path
          description: Traveller's ID
          required: true
          type: string
      responses:
        '204':
          description: Traveller deleted
  '/traveller/{travellerId}/wallet':
    get:
      tags:
        - wallet
      summary: Get information on traveller's wallet
      description: |
        Retrieve information on the payment means available in the wallet of
        the traveller with the provided traveller identifier.
      operationId: getTravellerWalletInformation
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerId
          in: path
          description: Traveller's ID
          required: true
          type: string
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/walletInformation'
  '/traveller/{travellerId}/wallet/transaction':
    post:
      tags:
        - wallet
      summary: Perform transaction on traveller's wallet
      description: |
        Create a transaction on the specified payment means of the traveller
        with the specified traveller identifier, optionally first making a
        reservation of the amount before the transaction is finally committed.
      operationId: postTravellerWalletTransactionRequest
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerId
          in: path
          description: Traveller's ID
          required: true
          type: string
        - name: walletTransaction
          in: body
          description: Transaction object
          required: true
          schema:
            $ref: '#/definitions/walletTransactionRequest'
      responses:
        '201':
          description: Successful operation
          schema:
            $ref: '#/definitions/transactionInformation'
          headers:
            location:
              description: URL of the created transaction
              type: string
              format: uri
        '403':
          description: Forbidden, transaction denied
        '409':
          description: Payment denied by payment service.
  '/traveller/{travellerId}/wallet/transaction/{transactionId}':
    get:
      tags:
        - wallet
      summary: Get information on traveller's wallet transaction
      description: |
        Retrieve information on a specific transaction with the given
        transaction identifier made on a wallet tied to the traveller with the
        given traveller identifier.
      operationId: getTransactionInformation
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerId
          in: path
          description: Traveller's ID
          required: true
          type: string
        - name: transactionId
          in: path
          description: Transaction ID
          required: true
          type: string
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/transactionInformation'
    patch:
      tags:
        - wallet
      summary: Update transaction
      description: |
        Commit the specified transaction of the traveller with the
        specified traveller identifier, which has been previously created as a
        reservation of an amount, or revert a committed transaction by
        cancelling it.
      operationId: updateTransactionInformation
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerId
          in: path
          description: Traveller's ID
          required: true
          type: string
        - name: transactionId
          in: path
          description: Transaction ID
          required: true
          type: string
        - name: walletTransaction
          in: body
          description: Transaction object
          required: true
          schema:
            $ref: '#/definitions/transactionInformationUpdate'
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/transactionInformation'
        '403':
          description: Forbidden, transaction denied
  '/traveller/{travellerId}/notification':
    post:
      tags:
        - notification
      summary: Post notification to traveller
      description: |
        Post a notification to the traveller with the specified traveller 
        identifier.
      operationId: sendTravellerNotification
      consumes:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerId
          in: path
          description: Traveller's ID
          required: true
          type: string
        - name: travellerNotification
          in: body
          description: Traveller notification message
          required: true
          schema:
            $ref: '#/definitions/travellerNotification'
      responses:
        '201':
          description: Successful operation
          headers:
            location:
              description: URL of created notification
              type: string
              format: uri
  '/traveller/{travellerId}/notification/{notificationId}':
    get:
      tags:
        - notification
      summary: Get notification sent to traveller
      description: |
        Retrieve information on a notification previously posted to the
        traveller with the specified traveller identifier.
      operationId: getTravellerNotification
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: travellerId
          in: path
          description: Traveller's ID
          required: true
          type: string
        - name: notificationId
          in: path
          description: Notification ID
          required: true
          type: string
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/travellerNotification'
  '/token/{tokenId}/productSets':
    get:
      tags:
        - token
      summary: Get information on products sets tied to token
      description: |
        Retrieve information on the set of products which has been tied to a
        token with the specified token identifier.
      operationId: getTokenProductSetInformation
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: tokenId
          in: path
          description: Token ID
          required: true
          type: string
        - name: all
          in: query
          description: |
            Include product sets that normally are excluded, like
            archived products and products in a final state.
          required: false
          type: boolean
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/productSets'
  '/token/{tokenId}/challenge':
    get:
      tags:
        - token
      summary: Get challenge to authenticate token
      description: |
        Retrieve a challenge to be used as authentication input for verifying
        possession of private key of a token.
      operationId: getTokenChallenge
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: tokenId
          in: path
          description: Token ID
          required: true
          type: string
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/tokenChallenge'
  '/token/{tokenId}/ticket':
    post:
      tags:
        - token
      summary: Purchase ticket on behalf of traveller
      description: |
        Make a purchase of the product with the provided product identifier,
        from the participant with the given participant identifier, on behalf
        of the traveller registered as holder of the given token ID.
      operationId: requestTokenTicket
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: tokenId
          in: path
          description: Token ID
          required: true
          type: string
        - name: purchaseRequest
          in: body
          description: Purchase request
          required: true
          schema:
            $ref: '#/definitions/purchaseRequest'
      responses:
        '201':
          description: Successful operation
          headers:
            location:
              description: URL of created ticket bundle
              type: string
              format: uri
          schema:
            $ref: '#/definitions/productSetInformation'
        '402':
          description: Payment declined
  '/token/{tokenId}/wallet':
    get:
      tags:
        - token
      summary: Get information on wallet tied to token
      description: |
        Retrieve information on the payment means in a wallet which has been 
        tied to a token with the specified token identifier.
      operationId: getTokenWalletInformation
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: tokenId
          in: path
          description: Token ID
          required: true
          type: string
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/walletInformation'
  '/token/{tokenId}/wallet/transaction':
    post:
      tags:
        - token
      summary: Perform transaction on wallet tied to token
      description: |
        Create a transaction with the given amount on the wallet tied to the
        token with the given token identifier, optionally providing information
        on the desired payment means.
      operationId: postTokenWalletTransactionRequest
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: tokenId
          in: path
          description: Token ID
          required: true
          type: string
        - name: walletTransaction
          in: body
          description: Transaction object
          required: true
          schema:
            $ref: '#/definitions/walletTransactionRequest'
      responses:
        '201':
          description: Successful operation
          schema:
            $ref: '#/definitions/transactionInformation'
          headers:
            location:
              description: URL of the created transaction
              type: string
              format: uri
        '403':
          description: Forbidden, transaction denied
        '409':
          description: Payment denied by payment service.
  '/token/{tokenId}/wallet/transaction/{transactionId}':
    get:
      tags:
        - token
      summary: Get information on transaction tied to token
      description: |
        Retrieve information on a specific transaction with the given
        transaction identifier made on a wallet tied to the token with the
        given token identifier.
      operationId: getTokenTransactionInformation
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: tokenId
          in: path
          description: Token ID
          required: true
          type: string
        - name: transactionId
          in: path
          description: Transaction ID
          required: true
          type: string
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/transactionInformation'
    patch:
      tags:
        - token
      summary: Update transaction tied to token
      description: |
        Commit the specified transaction of the token with the specified token
        identifier, which has been previously created as a reservation of an
        amount, or revert a committed transaction by cancelling it.
      operationId: updateTokenTransactionInformation
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: tokenId
          in: path
          description: Token ID
          required: true
          type: string
        - name: transactionId
          in: path
          description: Transaction ID
          required: true
          type: string
        - name: walletTransaction
          in: body
          description: Transaction object
          required: true
          schema:
            $ref: '#/definitions/transactionInformationUpdate'
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/transactionInformation'
        '403':
          description: Forbidden, transaction denied
  '/mtb/{issuerSignature}/productSets':
    get:
      tags:
        - mtb
      summary: Get information on product sets in MTB
      description: |
        Retrieve information on the product set(s) contained within a specific
        MTB with the provided Issuer Signature.
      operationId: getMTBProductSetInformation
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: issuerSignature
          in: path
          description: MTB Issuer Signature
          required: true
          type: string
          format: base64url
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/productSets'

  '/mtb/{issuerSignature}/activate':
    post:
      tags:
        - mtb
      summary: Activate MTB
      description: |
        Activate tickets contained within a MTB with the given Issuer
        Signature, possibly across multiple participants, by providing the
        Ticket Identifier of the ticket triggering the activation.
      operationId: activateMTB
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: issuerSignature
          in: path
          description: MTB issuer signature
          required: true
          type: string
          format: base64url
        - name: activationData
          in: body
          description: MTB activation data
          required: true
          schema:
            $ref: '#/definitions/activationData'
      responses:
        '204':
          description: Successful operation
        '404':
          description: MTB for issuer signature not found

  '/productSet':
    post:
      tags:
        - productSet
      summary: Recreate an equivalent product set
      description: |
        Recreate an equivalent product set to that of the given product set
        identifier, with the specified product set options, setting the status
        to recreated if successful.
      operationId: recreateProductSet
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: productSetId
          in: query
          description: Recreate product set from Product set ID
          required: true
          type: string
        - name: productSetOptions
          in: body
          description: Product set options
          schema:
            $ref: '#/definitions/productSetOptions'
      responses:
        '201':
          description: Successful operation
          schema:
            $ref: '#/definitions/productSetInformation'
          headers:
            location:
              description: URL of recreated product set
              type: string
              format: uri
        '403':
          description: Forbidden, recreation failed

  '/productSet/{productSetId}':
    get:
      tags:
        - productSet
      summary: Get information on product set
      description: |
        Retrieve information on a Product Set with the given product set
        identifier
      operationId: getProductSetInformation
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: productSetId
          in: path
          description: Product set ID
          required: true
          type: string
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/productSetInformation'
    delete:
      tags:
        - productSet
      summary: Delete a recreated product set
      operationId: deleteProductSetInformation
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: productSetId
          in: path
          description: Product set ID
          required: true
          type: string
      responses:
        '204':
          description: Product set deleted
    patch:
      tags:
        - productSet
      summary: Update a recreated product set
      operationId: updateProductSetInformation
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: productSetId
          in: path
          description: Product set ID
          required: true
          type: string
        - name: productSetInformation
          in: body
          description: Product set update
          required: true
          schema:
            $ref: '#/definitions/productSetInformationUpdate'
      responses:
        '200':
          description: Successful operation
          schema:
            $ref: '#/definitions/productSetInformation'

  '/ticketNotification':
    post:
      tags:
        - ticketNotification
      summary: Notification on ticket event
      description: |
        Post a ticket event notification, instructing the sales channel to
        update itself on the status of the ticket with the specified ticket
        identifier.
      operationId: createTicketNotification
      consumes:
        - application/json
      parameters:
        - name: X-BoB-AuthToken
          in: header
          description: JWT authentication token
          required: true
          type: string
        - name: ticketNotification
          in: body
          description: Ticket notification information
          schema:
            $ref: '#/definitions/ticketNotification'
      responses:
        '201':
          description: Created ok
        '403':
          description: Forbidden, creation of ticket event notification failed

definitions:

  ticketNotification:
    x-semantics:
      Transmodel:
        concepts:
          - "CUSTOMER PURCHASE PACKAGE (Sales Distribution MODEL)"
        definition: "A push notification about a change in the state or validity of a ticket, including participant ID, ticket ID, a URI to refresh the ticket, the cause of change, and the event timestamp."
        conditions:
          - "Partial match. ticketNotification aligns with a CUSTOMER PURCHASE PACKAGE status notification in Transmodel (SD). The cause enum (stateChange, validityChange, other) maps to state-change events in the CUSTOMER PURCHASE PACKAGE lifecycle."
    type: object
    properties:
      pid:
        description: Participant identifier
        type: integer
        format: int64
      ticketId:
        description: Ticket identifier
        type: string
      ticketLocation:
        description: Location URI to refresh ticket
        type: string
        format: uri
      cause:
        type: string
        enum:
          - stateChange
          - validityChange
          - other
      eventTime:
        description: |
          Time of the notification-triggering event as ISO 8601:2004 profile 
          extended format (MTS8, chapter 2.3)
        type: string
        format: date-time
        example: '2020-08-28T23:20:50.52Z'

  travellerNotification:
    x-semantics:
      Transmodel:
        concepts:
          - "No direct Transmodel equivalent"
        definition: "A push notification message sent to a traveller, with an identifier, status (queued/sent/acknowledged/discarded), language, message text, and expiry."
        conditions:
          - "No direct Transmodel equivalent. travellerNotification is a BoB-specific asynchronous messaging concept. Transmodel does not model push notification delivery states."
    type: object
    required:
      - notificationId
      - notificationStatus
      - notificationMessage
    properties:
      notificationId:
        type: string
      notificationStatus:
        type: string
        enum:
          - queued
          - sent
          - acknowledged
          - discarded
      notificationLang:
        description: Language of notification message (RFC 3066)
        type: string
        example: "sv-SE"
      notificationMessage:
        description: Notification message
        type: string
      notificationExpire:
        description: |
          Expire time stamp as ISO 8601:2004 profile extended format
          (MTS8, chapter 2.3)
        type: string
        format: date-time
        example: "1985-04-12T23:20:50.52Z"

  travellerInformation:
    x-semantics:
      Transmodel:
        concepts:
          - "TRANSPORT CUSTOMER (Sales Distribution MODEL)"
          - "CUSTOMER ACCOUNT (Sales Distribution MODEL)"
        definition: "The profile of a registered traveller, including identifier, name, date of birth, email, phone, photo URL, preferred languages, and a list of associated tokens."
        conditions:
          - "Exact match. travellerInformation maps to TRANSPORT CUSTOMER / CUSTOMER ACCOUNT in Transmodel (SD). travellerId=TRANSPORT CUSTOMER identifier; travellerName/DOB/email/phone are CUSTOMER ACCOUNT profile attributes; travellerTokens align with MEDIUM ACCESS DEVICE registrations."
    type: object
    required:
      - travellerId
    properties:
      travellerId:
        type: string
        example: 9ACFAB16-3A5C-4310-AB88-1E96E5EB34A4
      travellerName:
        description: Name
        type: string
        example: Anders Andersson
      travellerDOB:
        description: Date of birth
        type: string
        format: date
        example: "1984-01-01"
      travellerEmail:
        description: Email address
        type: string
        format: email
        example: "user@example.com"
      travellerPhone:
        description: Phone number (E.164)
        type: string
        pattern: "^\\+[1-9]\\d{1,14}$"
        example: "+4686001000"
      travellerPhotoURL:
        description: URL to photo
        type: string
        format: uri
        example: "https://example.com/CBC392F0-C026-4201-9ABA-56915A2B5478.png"
      travellerPrefLang:
        description: Ordered list of preferred languages (RFC 3066)
        type: array
        items:
          type: string
          example: "sv-SE"
      travellerTokens:
        description: List of tokens tied to the traveller
        type: array
        items:
          $ref: '#/definitions/travellerToken'

  travellerToken:
    x-semantics:
      Transmodel:
        concepts:
          - "MEDIUM ACCESS DEVICE (Sales Distribution MODEL)"
        definition: "A token registered to a traveller, identified by a participant PID, token serial number, and token identifier."
        conditions:
          - "Partial match. travellerToken corresponds to a MEDIUM ACCESS DEVICE registration in Transmodel (SD). The pid and serial fields align with the issuing organisation and device serial attributes; tokenId is the BoB-specific token handle."
    type: object
    additionalProperties: true
    required:
      - pid
      - tokenId
    properties:
      pid:
        $ref: '#/definitions/pid'
      serial:
        $ref: '#/definitions/serial'
      tokenId:
        type: string

  pid:
    x-semantics:
      Transmodel:
        concepts:
          - "No direct Transmodel equivalent"
        definition: "A BoB Participant Identifier — a numeric identifier assigned to each organisation participating in the BoB ecosystem."
        conditions:
          - "No direct Transmodel equivalent as a standalone scalar type. The concept maps to the identifier of an ORGANISATION (Access Management MODEL) or OPERATOR in Transmodel, but Transmodel does not define a typed PID scalar."
    description: Participant Identifier
    type: integer
    format: int64
    example: 1

  serial:
    x-semantics:
      Transmodel:
        concepts:
          - "No direct Transmodel equivalent"
        definition: "A token serial number — a numeric sequence identifier for a contactless token (e.g. smart card) within a participant's namespace."
        conditions:
          - "No direct Transmodel equivalent as a standalone scalar type. Conceptually aligns with a MEDIUM ACCESS DEVICE serial number attribute in Transmodel (SD)."
    description: Token serial number
    type: integer
    format: int64
    example: 42

  walletInformation:
    x-semantics:
      Transmodel:
        concepts:
          - "FARE CONTRACT (Sales Distribution MODEL)"
        definition: "The wallet state of a traveller, comprising a list of payment means available for purchasing products."
        conditions:
          - "Partial match. walletInformation aligns with a FARE CONTRACT or CUSTOMER ACCOUNT payment record in Transmodel (SD). The list of paymentMeans partially aligns with PAYMENT METHOD entries in a FARE CONTRACT."
    type: object
    additionalProperties: true
    required:
      - paymentMeans
    properties:
      paymentMeans:
        type: array
        items:
          $ref: '#/definitions/paymentMeansItem'

  paymentMeansItem:
    x-semantics:
      Transmodel:
        concepts:
          - "PAYMENT METHOD (Sales Distribution MODEL)"
        definition: "A payment means registered to a traveller, describing its type (purse/paymentcard/mobile/invoice/other), currency, balance, transaction limits, and preference order."
        conditions:
          - "Partial match. paymentMeansItem aligns with a PAYMENT METHOD or FARE CONTRACT ENTRY in Transmodel (SD). The type enum, currency, balance, and limit fields map to payment instrument attributes; tokenProofRequired is a BoB-specific security field."
    type: object
    properties:
      id:
        description: Identifier of the payment means (unique per traveller)
        type: string
        example: 3D456793-6384-4E50-AF00-636A2520065E
      preference:
        description: |
          Traveller's preference of the payment means (lower number means
          higher preference. Zero indicates no preference.)
        type: number
        example: 0
      name:
        description: User-distinguishable name of the payment means
        type: string
        example: Kundkassa
      type:
        description: Type of payment means
        type: string
        enum:
          - purse
          - paymentcard
          - mobile
          - invoice
          - other
      currency:
        description: Payment currency (ISO 4217)
        type: string
        example: SEK
      limitPIN:
        description: Transaction limit if used in combination with PIN
        type: number
        example: 10000
      limit:
        description: Transaction limit if used without PIN
        type: number
        format: float
        example: 200.0
      tokenProofRequired:
        description: True if token proof is required for all transactions
        type: boolean
        example: true
      balance:
        description: Current balance (if available)
        type: number
        format: float
        example: 200.0
      depositAllowed:
        description: True if deposit (negative amount) is allowed, otherwise false
        type: boolean

  walletTransactionRequest:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES TRANSACTION (Sales Distribution MODEL)"
        definition: "A request to perform a wallet (payment means) transaction, specifying the amount, currency, payment means, optional PIN, and optional token proof for authentication."
        conditions:
          - "Partial match. walletTransactionRequest initiates a SALES TRANSACTION (SD) against a payment means. The tokenProof field is a BoB-specific authentication artefact with no Transmodel counterpart."
    type: object
    required:
      - transactionReference
      - amount
      - currency
    properties:
      transactionReference:
        description: Requestor's transaction reference
        type: string
      description:
        description: User-distinguishable description of the transaction
        type: string
      amount:
        description: Total amount of the transaction (can be negative)
        type: number
        format: float
        example: 120.0
      currency:
        description: Currency code (ISO 4217)
        type: string
      paymentMeansId:
        description: |
          Identifier of the payment means to use. If no paymentMeansId is
          given, the payment means with the highest preference and available
          funds will be used.
        type: string
        example: 3D456793-6384-4E50-AF00-636A2520065E
      paymentMeansPIN:
        description: User-provided PIN-code
        type: string
        pattern: "^\\d{4,}$"
        example: "1234"
      transactionCommit:
        description: |
          Set to false if transaction should be created as a reservation of
          amount, true if it should be finalised in one request (default).
        type: boolean
      tokenProof:
        $ref: '#/definitions/tokenProof'

  transactionInformation:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES TRANSACTION (Sales Distribution MODEL)"
        definition: "A transaction record for a wallet payment, including identifier, requestor reference, amount, currency, payment means, status, expiry, and cancellable-until timestamps."
        conditions:
          - "Partial match. transactionInformation corresponds to a SALES TRANSACTION record in Transmodel (SD). The status lifecycle (pending/committed/cancelled) and expiry fields are BoB-specific payment lifecycle attributes not fully modelled in Transmodel."
    type: object
    required:
      - id
      - transactionReference
      - amount
      - currency
    properties:
      id:
        description: The transaction identifier
        type: string
      transactionReference:
        description: Requestor's transaction reference
        type: string
      description:
        description: User-distinguishable description of the transaction
        type: string
      amount:
        description: Total amount of the transaction (can be negative)
        type: number
        format: float
      currency:
        description: Currency code (ISO 4217)
        type: string
      paymentMeansId:
        description: Identifier of the payment means to use
        type: string
      status:
        $ref: '#/definitions/transactionStatus'
      expire:
        description: Timestamp when a pending transaction expires and can no longer be committed, as
          ISO 8601:2004 profile extended format (MTS8, chapter 2.3).
        type: string
        format: date-time
        example: "2018-11-13T20:20:39+00:00"
      cancellableExpire:
        description: Timestamp when a comitted transaction can no longer be cancelled, as
          ISO 8601:2004 profile extended format (MTS8, chapter 2.3).
        type: string
        format: date-time
        example: '2018-11-13T20:20:39+00:00'
      tokenProof:
        $ref: '#/definitions/tokenProof'

  transactionInformationUpdate:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES TRANSACTION (Sales Distribution MODEL)"
        definition: "An update to a wallet transaction, carrying the new status (committed/cancelled) and an optional requestor reference."
        conditions:
          - "Partial match. transactionInformationUpdate represents a SALES TRANSACTION status update in Transmodel (SD)."
    type: object
    properties:
      status:
        $ref: '#/definitions/transactionStatusUpdate'
      transactionReference:
        description: Requestor's transaction reference
        type: string

  transactionStatus:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES TRANSACTION (Sales Distribution MODEL)"
        definition: "The lifecycle status of a wallet transaction: pending, committed, or cancelled."
        conditions:
          - "Partial match. transactionStatus maps to SALES TRANSACTION state in Transmodel (SD). The pending/committed/cancelled states align with SALES TRANSACTION lifecycle states."
    description: Transaction status
    type: string
    enum:
      - pending
      - commited
      - cancelled
    example: "pending"

  transactionStatusUpdate:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES TRANSACTION (Sales Distribution MODEL)"
        definition: "The set of valid target statuses when updating a wallet transaction: committed or cancelled."
        conditions:
          - "Partial match. transactionStatusUpdate is the writable subset of transactionStatus; aligns with SALES TRANSACTION lifecycle transitions in Transmodel (SD)."
    description: Transaction status
    type: string
    enum:
      - commited
      - cancelled
    example: "commited"

  purchaseRequest:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES TRANSACTION (Sales Distribution MODEL)"
        definition: "A request to purchase a product, specifying the participant, product identifier, and a requestor transaction reference."
        conditions:
          - "Partial match. purchaseRequest initiates a SALES TRANSACTION for a FARE PRODUCT / SALES OFFER PACKAGE in Transmodel (SD)."
    type: object
    properties:
      pid:
        $ref: '#/definitions/pid'
      productId:
        description: Product ID to purchase
        type: string
      transactionReference:
        description: Requestor's transaction reference
        type: string

  tokenChallenge:
    x-semantics:
      Transmodel:
        concepts:
          - "No direct Transmodel equivalent"
        definition: "A cryptographic challenge issued to a token for authentication, encoded as a base64url string."
        conditions:
          - "No direct Transmodel equivalent. tokenChallenge is a BoB-specific challenge-response authentication artefact for contactless tokens; Transmodel does not model cryptographic challenges."
    type: object
    required:
      - authenticationChallenge
    properties:
      authenticationChallenge:
        description: The challenge to be used as authentication input
        type: string
        format: base64url

  tokenProof:
    x-semantics:
      Transmodel:
        concepts:
          - "No direct Transmodel equivalent"
        definition: "A token authentication proof comprising the traveller token reference, the challenge used, and the signed response from the token."
        conditions:
          - "No direct Transmodel equivalent. tokenProof is a BoB-specific cryptographic proof of token possession; Transmodel does not model challenge-response proofs."
    type: object
    properties:
      travellerToken:
        $ref: '#/definitions/travellerToken'
      authenticationChallenge:
        description: The challenge used as authentication input to the token
        type: string
        format: base64url
      authenticationResponse:
        description: The signed authentication input recieved from the token
        type: string
        format: base64url

  productSets:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES OFFER PACKAGE (Sales Distribution MODEL)"
        definition: "A list of product set information records representing products available to or held by a traveller."
        conditions:
          - "Partial match. productSets is a collection that aligns with a set of SALES OFFER PACKAGEs or CUSTOMER PURCHASE PACKAGEs in Transmodel (SD)."
    description: List of product information
    type: array
    items:
      $ref: '#/definitions/productSetInformation'

  productSetOptions:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES OFFER PACKAGE (Sales Distribution MODEL)"
        definition: "Options for configuring a product set purchase, specifying traveller categories and property assignments."
        conditions:
          - "Partial match. productSetOptions captures the traveller composition and property selections when purchasing a SALES OFFER PACKAGE in Transmodel (SD)."
    type: object
    properties:
      travellersPerCategory:
        $ref: '#/definitions/travellersPerCategory'
      productProperties:
        description: List of product properties assignments
        type: array
        items:
          $ref: '#/definitions/productProperty'

  productProperty:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES OFFER PACKAGE ELEMENT (Sales Distribution MODEL)"
        definition: "A name-value pair representing a configurable property assignment on a product."
        conditions:
          - "Partial match. productProperty aligns with a SALES OFFER PACKAGE ELEMENT attribute assignment in Transmodel (SD), where a named property and its value correspond to an element's characteristic."
    description: Product property assignments
    type: object
    required:
      - name
      - value
    properties:
      name:
        description: Product property name
        type: string
      value:
        description: Product property value
        type: string

  productPropertyDeclaration:
    x-semantics:
      Transmodel:
        concepts:
          - "SALES OFFER PACKAGE ELEMENT (Sales Distribution MODEL)"
        definition: "A declaration of a configurable product property, specifying its name, type, default value, mandatory flag, description, optional surcharges, grouping, and allowed values."
        conditions:
          - "Partial match. productPropertyDeclaration corresponds to the schema/declaration of a SALES OFFER PACKAGE ELEMENT property in Transmodel (SD). The surcharges array aligns with FARE PRICE entries for the element."
    description: |
      Product properties declaration. Contains identifier, type, description
      and an optional default value.
    type: object
    required:
      - name
    properties:
      name:
        description: Product property name
        type: string
      default:
        description: Product property default value
        type: string
      mandatory:
        description: Product property prerequisite for product
        type: boolean
      description:
        description: Product property description
        type: string
      type:
        description: Product property type (primitive json types)
        type: string
      surcharges:
        description: |
          Product property surcharges. Can be several entries when surcharges include product property surcharges, discounts, different
          currencies or different VAT percentages. Total cost is the sum of all entries for any given currency.
          Discounts have negative amounts.
        type: array
        items:
          $ref: '#/definitions/fare'
      group:
        description: Product property group. Used for grouping properties together.
        type: string
      values:
        description: List of possible values, with description and optional surcharges.
        type: array
        items:
          type: object
          required:
            - value
            - description
          properties:
            value:
              type: string
              description: Product property value
            description:
              type: string
              description: Description of the product property value
            surcharges:
              type: array
              items:
                $ref: '#/definitions/fare'

  travellersPerCategory:
    x-semantics:
      Transmodel:
        concepts:
          - "GROUP OF PASSENGERS (Sales Distribution MODEL)"
        definition: "A list of traveller-per-category items describing the composition of a group travelling together."
        conditions:
          - "Partial match. travellersPerCategory maps to a GROUP OF PASSENGERS in Transmodel (SD), where each item represents the count of passengers in a given USER PROFILE category."
    description: Information about all travellers
    type: array
    items:
      $ref: '#/definitions/travellersPerCategoryItem'

  travellersPerCategoryItem:
    x-semantics:
      Transmodel:
        concepts:
          - "GROUP OF PASSENGERS (Sales Distribution MODEL)"
          - "USER PROFILE (Sales Distribution MODEL)"
        definition: "A single traveller-category entry specifying the category code, class of service, number of travellers, optional individual traveller identifiers, seating reservations, and a description."
        conditions:
          - "Partial match. travellersPerCategoryItem maps to an entry in a GROUP OF PASSENGERS in Transmodel (SD), with cat/cls aligning with USER PROFILE and class-of-service attributes."
    type: object
    required:
      - tra
    properties:
      cat:
        description: Traveller category
        type: string
        example: "a"
      cls:
        description: Class of service
        type: string
      tra:
        description: Number of travellers
        type: number
        example: 1
      trs:
        description: Traveller Identifiers
        type: array
        items:
          type: string
      sts:
        description: Seating reservations
        type: array
        items:
          type: string
      description:
        description: Traveller description
        type: string

  productSetInformation:
    x-semantics:
      Transmodel:
        concepts:
          - "CUSTOMER PURCHASE PACKAGE (Sales Distribution MODEL)"
        definition: "A product set held by a traveller, including its identifier, ticket bundles, title, description, planned rides, validity conditions, temporal and spatial constraints, status, and transaction reference."
        conditions:
          - "Partial match. productSetInformation corresponds to a CUSTOMER PURCHASE PACKAGE in Transmodel (SD). productSetId=CUSTOMER PURCHASE PACKAGE identifier; ticketBundles=issued CUSTOMER PURCHASE PACKAGE ELEMENTs; status aligns with CUSTOMER PURCHASE PACKAGE lifecycle."
    description: Product Set Information
    type: object
    required:
      - productSetId
    properties:
      productSetId:
        description: Product Set Identifier
        type: string
        example: "562FA1FA-A4C0-4D90-8E44-85E9040BDCC3"
      ticketBundles:
        type: array
        items:
          $ref: '#/definitions/ticketBundle'
      title:
        description: Product set title
        type: string
        example: "My fine ticket"
      description:
        description: Ticket description
        type: string
      rides:
        description: Ordered list of planned rides
        type: array
        items:
          $ref: '#/definitions/ride'
      conditions:
        description: |
          A list of conditions, with a logical AND between them.
        type: array
        items:
          $ref: '#/definitions/productCondition'
      temporalConstraints:
        $ref: '#/definitions/temporalConstraints'
      spatialConstraints:
        type: array
        items:
          $ref: '#/definitions/spatialConstraint'
      status:
        $ref: '#/definitions/productSetStatus'
      transactionReference:
        description: Requestor's transaction reference
        type: string

  productSetInformationUpdate:
    x-semantics:
      Transmodel:
        concepts:
          - "CUSTOMER PURCHASE PACKAGE (Sales Distribution MODEL)"
        definition: "An update payload for a product set, carrying the new lifecycle status and an optional transaction reference."
        conditions:
          - "Partial match. productSetInformationUpdate represents a CUSTOMER PURCHASE PACKAGE lifecycle status transition in Transmodel (SD)."
    description: Product Set Information Update
    type: object
    required:
      - status
    properties:
      status:
        $ref: '#/definitions/productSetStatusUpdate'
      transactionReference:
        description: Requestor's transaction reference
        type: string

  productSetStatus:
    x-semantics:
      Transmodel:
        concepts:
          - "CUSTOMER PURCHASE PACKAGE (Sales Distribution MODEL)"
        definition: "The lifecycle status of a product set: activated, expired, issued, recreated, refunded, revoked, or suspended."
        conditions:
          - "Partial match. productSetStatus maps to the state of a CUSTOMER PURCHASE PACKAGE in Transmodel (SD). The enum values correspond to CUSTOMER PURCHASE PACKAGE lifecycle transitions."
    description: Product set status
    type: string
    enum:
      - activated
      - expired
      - issued
      - recreated
      - refunded
      - revoked
      - suspended
    example: "issued"

  productSetStatusUpdate:
    x-semantics:
      Transmodel:
        concepts:
          - "CUSTOMER PURCHASE PACKAGE (Sales Distribution MODEL)"
        definition: "The writable subset of product set lifecycle statuses: activated, issued, refunded, or revoked."
        conditions:
          - "Partial match. productSetStatusUpdate is the subset of CUSTOMER PURCHASE PACKAGE lifecycle transitions that may be written by an API caller in Transmodel (SD)."
    description: Product set status (updates)
    type: string
    enum:
      - activated
      - issued
      - refunded
      - revoked
    example: "issued"

  ticketBundle:
    x-semantics:
      Transmodel:
        concepts:
          - "CUSTOMER PURCHASE PACKAGE (Sales Distribution MODEL)"
        definition: "A bundle of tickets issued by a single participant, including their fares, a bundle title, description, and bundle identifier."
        conditions:
          - "Partial match. ticketBundle maps to a group of CUSTOMER PURCHASE PACKAGE ELEMENTs issued by one OPERATOR (participant) in Transmodel (SD). The fares array aligns with FARE PRICE entries for those elements."
    description: Tickets issued by a participant
    type: object
    required:
      - pid
      - tickets
    properties:
      pid:
        $ref: '#/definitions/pid'
      tickets:
        type: array
        items:
          $ref: '#/definitions/ticket'
      fares:
        description: |
          List of all fares for the product set.
        type: array
        items:
          $ref: '#/definitions/fare'
      title:
        description: Product bundle title
        type: string
      description:
        description: Product bundle description
        type: string
      ticketBundleId:
        type: string

  ticket:
    x-semantics:
      Transmodel:
        concepts:
          - "CUSTOMER PURCHASE PACKAGE ELEMENT (Sales Distribution MODEL)"
        definition: "An individual ticket within a bundle, identified by its ticket identifier, product category, fare category, traveller composition, and product properties."
        conditions:
          - "Partial match. ticket maps to a CUSTOMER PURCHASE PACKAGE ELEMENT in Transmodel (SD). ticketId=element identifier; productCategoryId/fareCategoryId align with FARE PRODUCT and FARE CLASS categorisation."
    type: object
    required:
      - ticketId
    properties:
      title:
        description: Short description of ticket
        type: string
      description:
        description: Long description of ticket
        type: string
      ticketId:
        description: Ticket Identifier
        type: string
        example: "7851FD3A-9DA6-48EE-B0DD-6958752539B2"
      productCategoryId:
        description: Product category identifier
        type: string
      productCategoryDescription:
        description: Product category description
        type: string
      fareCategoryId:
        description: Fare category identifier
        type: string
      fareCategoryDescription:
        description: Fare category description
        type: string
      travellersPerCategory:
        $ref: '#/definitions/travellersPerCategory'
      productProperties:
        type: array
        items:
          $ref: '#/definitions/productProperty'
      availableProductProperties:
        type: array
        items:
          $ref: '#/definitions/productPropertyDeclaration'

  temporalConstraints:
    x-semantics:
      Transmodel:
        concepts:
          - "USAGE VALIDITY PERIOD (Sales Distribution MODEL)"
          - "VALIDITY CONDITION (Sales Distribution MODEL)"
        definition: "The temporal validity constraints on a product set, including issue time, start/end of validity, relative validity duration, latest activation, activation period, number/remaining activations, and suspension expiry."
        conditions:
          - "Exact match. temporalConstraints maps directly to USAGE VALIDITY PERIOD / VALIDITY CONDITION in Transmodel (SD). startOfValidity/endOfValidity=USAGE VALIDITY PERIOD bounds; relativeValidity=duration; numberOfActivations/remainingActivations=FREQUENCY OF USE limits."
    description: Bundle time constraints
    type: object
    required:
      - timeOfIssue
    properties:
      timeOfIssue:
        description: Date and time for when product set was issued, as
          ISO 8601:2004 profile extended format (MTS8, chapter 2.3)
        type: string
        format: date-time
        example: "2018-11-13T20:20:39+00:00"
      startOfValidity:
        description: Date and time for start of validity, as
          ISO 8601:2004 profile extended format (MTS8, chapter 2.3)
        type: string
        format: date-time
        example: "2018-11-13T20:20:39+00:00"
      endOfValidity:
        description: Date and time for end of validity, as
          ISO 8601:2004 profile extended format (MTS8, chapter 2.3)
        type: string
        format: date-time
        example: "2018-11-13T20:20:39+00:00"
      relativeValidity:
        description: Validity period in ISO 8601:2004 duration format (MTS8, chapter 2.4)
        type: string
        example: "PT24H"
      latestActivation:
        description: Date and time for latest activation, as
          ISO 8601:2004 profile extended format (MTS8, chapter 2.3)
        type: string
        format: date-time
        example: "2018-11-13T20:20:39+00:00"
      latestActivationExpire:
        description: Date and time for expiry of latest activation, as
          ISO 8601:2004 profile extended format (MTS8, chapter 2.3)
        type: string
        format: date-time
        example: "2018-11-14T20:20:39+00:00"
      numberOfActivations:
        description: |
          Initial value for number of activations
        type: integer
        format: int64
        example: 1
      remainingActivations:
        description: |
          Remaining number of times the product set can be activated
        type: integer
        format: int64
        example: 1
      activationPeriod:
        description: |
          Period in which the product may be activated. For single activation
          products this is the same as the relativeValidity and for multi
          activation products this is the period in which all activations need
          to be done. The activation period starts at startOfValidity and is
          given in ISO 8601:2004 duration format (MTS8, chapter 2.4).
        type: string
        example: "P60D"
      suspensionExpire:
        description: |
          If the product set is suspended, time when the suspension expires.
        type: string
        format: date-time
        example: "2021-06-14T22:00:00+02:00"

  spatialConstraint:
    x-semantics:
      Transmodel:
        concepts:
          - "GEOGRAPHICAL VALIDITY (Sales Distribution MODEL)"
        definition: "A spatial constraint on a product set, specifying a named area and a union of geographical areas (circles, polygons, polylines)."
        conditions:
          - "Partial match. spatialConstraint maps to GEOGRAPHICAL VALIDITY in Transmodel (SD), which defines the spatial scope within which a SALES OFFER PACKAGE or FARE PRODUCT is valid."
    description: Product set spatial constraints
    type: object
    properties:
      name:
        description: Other spatial constraint (text)
        type: string
      areas:
        type: array
        items:
          $ref: '#/definitions/geoAreas'

  geoAreas:
    x-semantics:
      Transmodel:
        concepts:
          - "ZONE (Common Geometry MODEL)"
        definition: "A union of geographical areas defined by circles, polygons, and/or polylines."
        conditions:
          - "Partial match. geoAreas maps to a ZONE boundary geometry in Transmodel (CG). The union of circles/polygons/polylines corresponds to the shape definition of a TOPOGRAPHIC PLACE or FARE ZONE."
    type: object
    description: Geographical areas (union)
    properties:
      circles:
        type: array
        items:
          $ref: '#/definitions/circle'
      polygons:
        type: array
        items:
          $ref: '#/definitions/polygon'
      polylines:
        type: array
        items:
          $ref: '#/definitions/polyline'

  circle:
    x-semantics:
      Transmodel:
        concepts:
          - "ZONE (Common Geometry MODEL)"
        definition: "A circular geographical area defined by a centre point and radius in metres."
        conditions:
          - "Partial match. circle maps to a circular ZONE boundary in Transmodel (CG)."
    description: Geographical circle (area)
    type: object
    required:
      - center
      - radius
    properties:
      center:
        description: Center of circle
        $ref: '#/definitions/geoPosition'
      radius:
        description: Radius of circle in meters
        type: number
        format: double

  polygon:
    x-semantics:
      Transmodel:
        concepts:
          - "ZONE (Common Geometry MODEL)"
        definition: "A polygonal geographical area defined as an ordered list of geo-positions."
        conditions:
          - "Partial match. polygon maps to a polygonal ZONE boundary in Transmodel (CG)."
    description: Geographical polygon (area)
    type: array
    items:
      $ref: '#/definitions/geoPosition'

  polyline:
    x-semantics:
      Transmodel:
        concepts:
          - "ROUTE LINK (Transport Pattern MODEL)"
        definition: "A geographical path defined as an ordered list of geo-positions."
        conditions:
          - "Partial match. polyline maps to a ROUTE LINK path geometry in Transmodel (TP), representing a linear geographical constraint."
    description: Geographical polygonal path (line)
    type: array
    items:
      $ref: '#/definitions/geoPosition'

  geoPosition:
    x-semantics:
      Transmodel:
        concepts:
          - "LOCATION (Common Geometry MODEL)"
        definition: "A WGS84 decimal geo-position with latitude, longitude, and optional altitude."
        conditions:
          - "Exact match. geoPosition maps directly to LOCATION in Transmodel (CG), which represents a point defined by WGS84 coordinates."
    description: Geopos (WGS84 decimal)
    type: object
    required:
      - lat
      - long
    properties:
      lat:
        description: Latitude
        type: number
        format: double
      long:
        description: Longitude
        type: number
        format: double
      alt:
        description: Altitude
        type: number
        format: double

  productCondition:
    x-semantics:
      Transmodel:
        concepts:
          - "VALIDITY CONDITION (Sales Distribution MODEL)"
        definition: "A usage condition on a product, defined by a category (e.g. temporal, spatial) and a human-readable text description."
        conditions:
          - "Partial match. productCondition maps to a VALIDITY CONDITION in Transmodel (SD). The category/text pair aligns with a named VALIDITY CONDITION and its prose description."
    description: |
      A product condition. For recommended categories, see JSON schema
      in https://bitbucket.org/samtrafiken/bob-schema-product-conditions.
    type: object
    required:
      - category
      - text
    properties:
      category:
        type: string
        example: "temporal"
      text:
        type: string
        example: "Valid on weekdays at hours 09-15 and 18-04"

  ride:
    x-semantics:
      Transmodel:
        concepts:
          - "SERVICE JOURNEY (Timetable Information MODEL)"
          - "CUSTOMER RESERVATION (Sales Distribution MODEL)"
        definition: "A planned ride within a journey, specifying boarding and alighting places, a service journey reference, and departure/arrival date-times."
        conditions:
          - "Partial match. ride maps to a SERVICE JOURNEY leg in Transmodel (TI), with boardingPlace/alightingPlace corresponding to SCHEDULED STOP POINTs and serviceJourneyReference to a SERVICE JOURNEY identifier. The reservation aspect aligns with CUSTOMER RESERVATION (SD)."
    description: A ride within a journey
    type: object
    properties:
      boardingPlace:
        $ref: '#/definitions/place'
      alightingPlace:
        $ref: '#/definitions/place'
      serviceJourneyReference:
        type: string
      departureDateTime:
        type: string
        format: date-time
      arrivalDateTime:
        type: string
        format: date-time

  place:
    x-semantics:
      Transmodel:
        concepts:
          - "SCHEDULED STOP POINT (Transport Pattern MODEL)"
        definition: "A place (stop) with a stop identifier, name, and optional geo-position."
        conditions:
          - "Exact match. place maps to a SCHEDULED STOP POINT in Transmodel (TP). stopId=SCHEDULED STOP POINT identifier; name=stop name; coordinate=LOCATION."
    type: object
    properties:
      stopId:
        type: string
      name:
        type: string
        example: "Halmstad C"
      coordinate:
        $ref: '#/definitions/geoPosition'

  fare:
    x-semantics:
      Transmodel:
        concepts:
          - "FARE PRICE (Sales Distribution MODEL)"
        definition: "Fare information comprising the amount (excluding VAT), currency, optional label and description, VAT amount, and VAT percentage."
        conditions:
          - "Partial match. fare maps to a FARE PRICE in Transmodel (SD). amount/currency align with FARE PRICE monetary attributes; vatAmount/vatPercent are BoB-specific tax fields not explicitly modelled in Transmodel."
    description: Fare information, amount, currency and VAT.
    type: object
    required:
      - amount
      - currency
      - vatAmount
      - vatPercent
    properties:
      amount:
        description: Fare cost excluding VAT
        type: number
        format: float
      currency:
        description: Currency code (ISO 4217)
        type: string
      label:
        description: Fare label
        type: string
        example: Group discount
      description:
        description: Fare description
        type: string
      vatAmount:
        description: VAT amount
        type: number
        format: float
      vatPercent:
        description: VAT percent
        type: number
        format: float

  activationData:
    x-semantics:
      Transmodel:
        concepts:
          - "CUSTOMER PURCHASE PACKAGE (Sales Distribution MODEL)"
        definition: "Data required to activate a product set ticket, specifying the activating participant, ticket identifier, optional activation timestamp, and optional event identifier."
        conditions:
          - "Partial match. activationData triggers a CUSTOMER PURCHASE PACKAGE activation event in Transmodel (SD). timeOfActivation aligns with the activation timestamp; pid/ticketId identify the OPERATOR and the CUSTOMER PURCHASE PACKAGE ELEMENT being activated."
    type: object
    required:
    - pid
    - ticketId
    properties:
      timeOfActivation:
        description: Date and time for activation (default NOW) as ISO 8601:2004 profile 
          extended format (MTS8, chapter 2.3)
        type: string
        format: date-time
      pid:
        description: Activating PID
        type: integer
        format: int64
      ticketId:
        description: Activating ticket identifier
        type: string
      eventId:
        description: Activating event identifier
        type: string