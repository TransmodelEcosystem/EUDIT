allOf:
  - $ref: ./Package.yaml
  - type: object
    properties:
      expiryTime:
        type: string
        format: date-time
      status:
        type: string
        enum: [locked, released, expired]