description: SEARCH OFFER DELIVERY — the root response object containing all offers matching the search request.
headers:
  Version:
    $ref: ../headers/version.yaml
  Content-Language:
    $ref: ../headers/content-language.yaml
  Expires:
    $ref: ../headers/expires.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/SearchOfferDelivery.yaml
