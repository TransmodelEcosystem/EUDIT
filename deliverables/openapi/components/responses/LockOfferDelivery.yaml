description: The response of the lock offer request, or the details of the locked offer.
headers:
  Version:
    $ref: ../headers/version.yaml
  Content-Language:
    $ref: ../headers/content-language.yaml
  Expires:
    $ref: ../headers/expires.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/LockedOffer.yaml